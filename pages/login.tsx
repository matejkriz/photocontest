import React from 'react';
import { Auth } from '../components/Auth';

const LoginPage = () => {
  return (
    <div>
      <h1>Přihlášení</h1>
      <Auth />
    </div>
  );
};

export default LoginPage;
